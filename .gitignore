# build / preview artifacts
temporary screenshots/

# OS cruft
.DS_Store

# node
node_modules/

# secrets / env — never commit Square tokens etc. (they live in Vercel env vars)
.env
.env.*
*.env

# claude code tooling
.claude/

# heavy video files - keep originals out, only ship compressed web versions
*.MP4
*.mov
*.MOV
*.mp4
!IMAGES/hero-bg.mp4

# editor / misc
CLAUDE (1).md
